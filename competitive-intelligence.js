// Phase 4D.24 — Competitive Intelligence, Rival Labs, Market Signals & Technology-Race Dynamics
const COMP_INTEL_VERSION=1;
const RIVAL_ARCHETYPES=[
  {id:'helios',name:'Helios Labs',focus:'frontier',capability:.78,efficiency:.54,enterprise:.38,talent:.74,compute:.82},
  {id:'forge',name:'Forge Systems',focus:'efficiency',capability:.61,efficiency:.83,enterprise:.58,talent:.62,compute:.68},
  {id:'citadel',name:'Citadel AI',focus:'enterprise',capability:.56,efficiency:.59,enterprise:.87,talent:.66,compute:.63}
];
const MARKET_SIGNALS=[
  {id:'benchmark',name:'Benchmark leak',dimension:'capability',delta:.07,confidence:.58},
  {id:'pricing',name:'Inference price cut',dimension:'efficiency',delta:.08,confidence:.82},
  {id:'enterprise',name:'Major enterprise win',dimension:'enterprise',delta:.07,confidence:.88},
  {id:'hiring',name:'High-profile hiring wave',dimension:'talent',delta:.06,confidence:.7},
  {id:'gpu',name:'Large compute reservation',dimension:'compute',delta:.08,confidence:.76}
];
function ensureCompetitiveIntel(){ensureInvestmentCommittee?.();state.competitiveIntel ||= {version:COMP_INTEL_VERSION,rivals:RIVAL_ARCHETYPES.map(r=>({...r})),signals:[],responses:[],intelConfidence:.55,racePressure:.42,lastSignalDay:null};evaluateCompetition()}
function ourPosition(){const frontier=Math.min(1,.45+(state.models?.length||0)*.03+(state.portfolioStrategy?.theme==='frontier'?.08:0));const efficiency=Math.min(1,.5+(state.orgMgmt?.executionCapacity||.7)*.3);const enterprise=Math.min(1,.48+(state.releaseGov?.customer?.trust||.6)*.35);const talent=Math.min(1,.42+(state.talent?.recruitingReputation||.6)*.45);const compute=Math.min(1,.4+Math.min(1,(state.quarterlyBoard?.computeCapacity||state.finance?.computeCapacity||0)/100)*.5);return {frontier,efficiency,enterprise,talent,compute}}
function strongestRival(){ensureCompetitiveIntel();return state.competitiveIntel.rivals.slice().sort((a,b)=>rivalStrength(b)-rivalStrength(a))[0]}
function rivalStrength(r){return r.capability*.27+r.efficiency*.2+r.enterprise*.2+r.talent*.15+r.compute*.18}
function evaluateCompetition(){if(!state.competitiveIntel)return;const ours=ourPosition(),rivals=state.competitiveIntel.rivals;const best=Math.max(...rivals.map(r=>rivalStrength(r))),us=ours.frontier*.27+ours.efficiency*.2+ours.enterprise*.2+ours.talent*.15+ours.compute*.18;state.competitiveIntel.racePressure=Math.max(0,Math.min(1,.5+(best-us)*.9));const sig=state.competitiveIntel.signals.slice(0,8);state.competitiveIntel.intelConfidence=sig.length?Math.max(.35,Math.min(.95,sig.reduce((n,s)=>n+s.confidence,0)/sig.length)):.55;if(state.quarterlyBoard)state.quarterlyBoard.executivePressure=Math.min(1,(state.quarterlyBoard.executivePressure||.5)+state.competitiveIntel.racePressure*.003)}
function generateMarketSignal(){ensureCompetitiveIntel();const rival=state.competitiveIntel.rivals[Math.floor(Math.random()*state.competitiveIntel.rivals.length)],s=MARKET_SIGNALS[Math.floor(Math.random()*MARKET_SIGNALS.length)],noise=(Math.random()-.5)*.04;const perceived=Math.max(-.02,s.delta+noise);rival[s.dimension]=Math.min(1,(rival[s.dimension]||.5)+s.delta*.45);state.competitiveIntel.signals.unshift({day:state.day||1,rivalId:rival.id,type:s.id,name:s.name,dimension:s.dimension,perceived,confidence:Math.max(.35,Math.min(.95,s.confidence+(Math.random()-.5)*.15))});state.competitiveIntel.signals=state.competitiveIntel.signals.slice(0,20);evaluateCompetition();save();render()}
function competitiveResponse(rivalId,response){ensureCompetitiveIntel();const r=state.competitiveIntel.rivals.find(x=>x.id===rivalId);if(!r)return;const c=state.competitiveIntel;if(response==='race'){state.portfolioStrategy.theme='frontier';state.portfolioStrategy.budgetM+=.5;state.talent.compensationPressure=Math.min(1,(state.talent.compensationPressure||.5)+.025);state.orgMgmt.executionCapacity=Math.max(.2,(state.orgMgmt.executionCapacity||.7)-.015)}else if(response==='differentiate'){state.portfolioStrategy.theme=r.focus==='enterprise'?'efficiency':'enterprise';if(state.releaseGov?.customer)state.releaseGov.customer.trust=Math.min(1,state.releaseGov.customer.trust+.01);c.intelConfidence=Math.min(.95,c.intelConfidence+.03)}else if(response==='recruit'){state.talent.recruitingReputation=Math.min(1,(state.talent.recruitingReputation||.6)+.02);state.talent.compensationPressure=Math.min(1,(state.talent.compensationPressure||.5)+.04);state.cash=Math.max(0,(state.cash||0)-250000)}else if(response==='compute'){if(state.finance)state.finance.computeCapacity=(state.finance.computeCapacity||0)+5;state.cash=Math.max(0,(state.cash||0)-500000)}else if(response==='hold'){c.intelConfidence=Math.min(.95,c.intelConfidence+.015);state.investmentCommittee.optionDiscipline=Math.min(1,(state.investmentCommittee.optionDiscipline||.5)+.01)}c.responses.unshift({day:state.day||1,rivalId,response});c.responses=c.responses.slice(0,20);evaluateCompetition();save();render()}
function advanceRivals(){ensureCompetitiveIntel();for(const r of state.competitiveIntel.rivals){const focus=r.focus==='frontier'?'capability':r.focus==='efficiency'?'efficiency':'enterprise';r[focus]=Math.min(1,r[focus]+.003+Math.random()*.004);r.talent=Math.min(1,r.talent+.0015);r.compute=Math.min(1,r.compute+.0015)}evaluateCompetition()}
function competitiveOpen(){ensureCompetitiveIntel();advanceRivals();state.view='competitiveIntel';save();render()}
function competitiveClose(){state.view='company';save();render()}
function renderCompetitiveIntel(){ensureCompetitiveIntel();const c=state.competitiveIntel,ours=ourPosition();return `<div class="ci-shell"><header class="ci-head"><div><div class="eyebrow">PHASE 4D.24 · COMPETITIVE INTELLIGENCE</div><h1>Rival Labs, Market Signals & Technology Race</h1><p>Interpret noisy external signals, distinguish real threats from hype, and choose where to race, differentiate, recruit, buy capacity, or wait.</p></div><button onclick="competitiveClose()">Return to company</button></header><section class="ci-summary"><div><span>Race pressure</span><b>${Math.round(c.racePressure*100)}%</b></div><div><span>Intel confidence</span><b>${Math.round(c.intelConfidence*100)}%</b></div><div><span>Signals tracked</span><b>${c.signals.length}</b></div><div><span>Responses</span><b>${c.responses.length}</b></div></section><section class="ci-card"><button onclick="generateMarketSignal()">Collect market signal</button></section><section class="ci-card"><h2>Competitive map</h2>${c.rivals.map(r=>`<div class="ci-rival"><div><b>${esc(r.name)}</b><span>${esc(r.focus)} focus · strength ${Math.round(rivalStrength(r)*100)}%</span><small>Capability ${Math.round(r.capability*100)} · efficiency ${Math.round(r.efficiency*100)} · enterprise ${Math.round(r.enterprise*100)} · talent ${Math.round(r.talent*100)} · compute ${Math.round(r.compute*100)}</small></div><div><button onclick="competitiveResponse('${r.id}','race')">Race frontier</button><button onclick="competitiveResponse('${r.id}','differentiate')">Differentiate</button><button onclick="competitiveResponse('${r.id}','recruit')">Recruit harder</button><button onclick="competitiveResponse('${r.id}','compute')">Secure compute</button><button onclick="competitiveResponse('${r.id}','hold')">Hold / learn</button></div></div>`).join('')}<div class="ci-you"><b>Your lab</b><span>Frontier ${Math.round(ours.frontier*100)} · efficiency ${Math.round(ours.efficiency*100)} · enterprise ${Math.round(ours.enterprise*100)} · talent ${Math.round(ours.talent*100)} · compute ${Math.round(ours.compute*100)}</span></div></section><section class="ci-card"><h2>Signal feed</h2>${c.signals.map(s=>{const r=c.rivals.find(x=>x.id===s.rivalId);return `<article><b>${esc(r?.name||s.rivalId)} · ${esc(s.name)}</b><span>${esc(s.dimension)} +${Math.round(s.perceived*100)} pts · confidence ${Math.round(s.confidence*100)}%</span></article>`}).join('')||'<p>No signals collected yet.</p>'}</section></div>`}
const ciBaseRender=render;render=function(){ensureCompetitiveIntel();if(state.view==='competitiveIntel'){document.getElementById('app').innerHTML=renderCompetitiveIntel();return}ciBaseRender();if(!state.started)return;const shell=document.querySelector('.game-shell');if(!shell)return;const b=document.createElement('button');b.className='ci-launch';b.onclick=competitiveOpen;b.innerHTML=`<span>COMPETITIVE INTELLIGENCE</span><b>${Math.round(state.competitiveIntel.racePressure*100)}% race pressure · ${Math.round(state.competitiveIntel.intelConfidence*100)}% confidence</b><small>Rivals · market signals · strategic response →</small>`;shell.insertBefore(b,shell.children[1]||null)};render();