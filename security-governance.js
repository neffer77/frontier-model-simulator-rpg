// Phase 4D.31 — Security Governance, Red Teaming, Insider Risk & Model Theft
const SECURITY_VERSION=1;
function ensureSecurity(){ensureResilience?.();state.securityGov ||= {version:SECURITY_VERSION,posture:.52,redTeamMaturity:.38,privilegedRisk:.42,modelProtection:.45,incidents:[],exercises:[],policy:'balanced',lastTickDay:null};assessSecurity()}
function assessSecurity(){if(!state.securityGov)return;const s=state.securityGov,people=(state.npcEmployees||[]).length||1,over=(state.npcEmployees||[]).filter(e=>(e.workload||0)>85).length/people,attr=(state.npcEmployees||[]).reduce((n,e)=>n+(e.attritionRisk||0),0)/(people*100);s.privilegedRisk=Math.max(.05,Math.min(.95,.22+over*.25+attr*.28+(1-(state.erm?.controlMaturity||.5))*.22));s.posture=Math.max(0,Math.min(1,(s.redTeamMaturity*.28+s.modelProtection*.32+(1-s.privilegedRisk)*.2+(state.erm?.controlMaturity||.5)*.2)));}
function setSecurityPolicy(p){ensureSecurity();if(!['fast','balanced','locked'].includes(p))return;state.securityGov.policy=p;if(p==='fast'){state.securityGov.modelProtection=Math.max(.1,state.securityGov.modelProtection-.04);state.orgMgmt&&(state.orgMgmt.executionCapacity=Math.min(1,(state.orgMgmt.executionCapacity||.7)+.015))}if(p==='locked'){state.securityGov.modelProtection=Math.min(1,state.securityGov.modelProtection+.06);state.orgMgmt&&(state.orgMgmt.executionCapacity=Math.max(.2,(state.orgMgmt.executionCapacity||.7)-.015))}assessSecurity();save();render()}
function runRedTeam(kind='tabletop'){ensureSecurity();const cost=kind==='full'?450000:180000;if((state.cash||0)<cost)return;state.cash-=cost;const gain=kind==='full'?.09:.045;state.securityGov.redTeamMaturity=Math.min(1,state.securityGov.redTeamMaturity+gain);state.securityGov.modelProtection=Math.min(1,state.securityGov.modelProtection+gain*.45);state.securityGov.exercises.unshift({day:state.day||1,kind,result:Math.round((state.securityGov.posture+gain)*100)});state.securityGov.exercises=state.securityGov.exercises.slice(0,10);assessSecurity();save();render()}
function triggerSecurityIncident(type){ensureSecurity();const defs={insider:{name:'Privileged insider exfiltration attempt',base:.58},weights:{name:'Model-weight theft attempt',base:.72},supply:{name:'Compromised build/dependency incident',base:.48},credential:{name:'Privileged credential compromise',base:.52}},d=defs[type];if(!d)return;const mitigation=state.securityGov.posture*.42+(state.resilience?.drMaturity||.4)*.12;const severity=Math.max(.08,Math.min(.95,d.base-mitigation+Math.random()*.18));state.securityGov.incidents.unshift({id:`SEC-${Date.now()}`,day:state.day||1,type,name:d.name,severity,status:'active',contained:false});state.securityGov.incidents=state.securityGov.incidents.slice(0,12);state.publicComms&&(state.publicComms.crisisPressure=Math.min(1,(state.publicComms.crisisPressure||0)+severity*.22));state.erm&&(state.erm.enterpriseRisk=Math.min(100,(state.erm.enterpriseRisk||0)+Math.round(severity*12)));save();render()}
function containSecurityIncident(id,mode){ensureSecurity();const i=state.securityGov.incidents.find(x=>x.id===id&&x.status==='active');if(!i)return;const costs={quiet:120000,forensic:380000,shutdown:900000},cost=costs[mode]||0;if((state.cash||0)<cost)return;state.cash-=cost;if(mode==='quiet'){i.severity=Math.max(.03,i.severity-.16);state.publicComms&&(state.publicComms.publicTrust=Math.max(0,(state.publicComms.publicTrust||.5)-.015))}if(mode==='forensic'){i.severity=Math.max(.03,i.severity-.32);state.securityGov.posture=Math.min(1,state.securityGov.posture+.025)}if(mode==='shutdown'){i.severity=Math.max(.01,i.severity-.5);state.orgMgmt&&(state.orgMgmt.executionCapacity=Math.max(.2,(state.orgMgmt.executionCapacity||.7)-.04))}i.contained=true;i.status='resolved';if(i.type==='weights'&&i.severity>.35){state.customerTrust=Math.max(0,(state.customerTrust||.6)-.04);state.legal&&(state.legal.discoveryBurden=Math.min(100,(state.legal.discoveryBurden||0)+5))}assessSecurity();save();render()}
function investModelProtection(){ensureSecurity();if((state.cash||0)<650000)return;state.cash-=650000;state.securityGov.modelProtection=Math.min(1,state.securityGov.modelProtection+.09);state.securityGov.privilegedRisk=Math.max(.05,state.securityGov.privilegedRisk-.025);state.orgMgmt&&(state.orgMgmt.executionCapacity=Math.max(.2,(state.orgMgmt.executionCapacity||.7)-.008));assessSecurity();save();render()}
function securityOpen(){ensureSecurity();state.view='securityGov';save();render()}
function securityClose(){state.view='company';save();render()}
function renderSecurityGov(){ensureSecurity();const s=state.securityGov;return `<div class="sg-shell"><header class="sg-head"><div><div class="eyebrow">PHASE 4D.31 · SECURITY GOVERNANCE</div><h1>Red Teaming, Insider Risk & Model Theft</h1><p>Protect frontier-model assets without paralyzing the company with controls.</p></div><button onclick="securityClose()">Return to company</button></header><section class="sg-summary"><div><span>Security posture</span><b>${Math.round(s.posture*100)}%</b></div><div><span>Red-team maturity</span><b>${Math.round(s.redTeamMaturity*100)}%</b></div><div><span>Privileged risk</span><b>${Math.round(s.privilegedRisk*100)}%</b></div><div><span>Model protection</span><b>${Math.round(s.modelProtection*100)}%</b></div></section><section class="sg-card"><h2>Security operating model</h2><div class="sg-actions"><button onclick="setSecurityPolicy('fast')">Move fast</button><button onclick="setSecurityPolicy('balanced')">Balanced</button><button onclick="setSecurityPolicy('locked')">Lock down</button><button onclick="investModelProtection()">Invest $0.65M in model protection</button><button onclick="runRedTeam('tabletop')">Red-team tabletop</button><button onclick="runRedTeam('full')">Full red-team exercise</button></div><p>Current policy: <b>${s.policy}</b></p></section><section class="sg-card"><h2>Threat simulation</h2><div class="sg-actions"><button onclick="triggerSecurityIncident('insider')">Simulate insider attempt</button><button onclick="triggerSecurityIncident('weights')">Simulate weight theft</button><button onclick="triggerSecurityIncident('supply')">Simulate supply-chain compromise</button><button onclick="triggerSecurityIncident('credential')">Simulate credential compromise</button></div></section><section class="sg-card"><h2>Security incidents</h2>${s.incidents.map(i=>`<article class="${i.status==='active'?'risk':''}"><b>${esc(i.name)}</b><span>Day ${i.day} · severity ${Math.round(i.severity*100)}% · ${i.status}</span>${i.status==='active'?`<div class="sg-actions"><button onclick="containSecurityIncident('${i.id}','quiet')">Quiet contain</button><button onclick="containSecurityIncident('${i.id}','forensic')">Forensic response</button><button onclick="containSecurityIncident('${i.id}','shutdown')">Emergency shutdown</button></div>`:''}</article>`).join('')||'<p>No security incidents recorded.</p>'}</section></div>`}
const sgBaseRender=render;render=function(){ensureSecurity();if(state.view==='securityGov'){document.getElementById('app').innerHTML=renderSecurityGov();return}sgBaseRender();if(!state.started)return;const shell=document.querySelector('.game-shell');if(!shell)return;const b=document.createElement('button');b.className='sg-launch';b.onclick=securityOpen;b.innerHTML=`<span>SECURITY GOVERNANCE</span><b>${Math.round(state.securityGov.posture*100)}% posture · ${Math.round(state.securityGov.privilegedRisk*100)}% insider risk</b><small>Red team · model protection · incident response →</small>`;shell.insertBefore(b,shell.children[1]||null)};render();