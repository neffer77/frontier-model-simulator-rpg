# P5.3.8 — Mail folder continuity through Run Monitor

Mail advice opened from Starred or Archive lost its folder after a linked Run Monitor reload. The regression failed on merged main `e850c792199b2570a41a216214f9b0e5c5acc609`: the outbound route omitted `/from/inbox`.

Mail now sends `<incident>/<view>/return/<thread>/from/<folder>`, encoding each segment. Run Monitor decodes the route and returns to `thread/<thread>/from/<folder>`. The existing session owner persists the detail; no second navigation store is added. The native render reconciliation retains both return fields. Unrelated opens clear them, and old links return through the original plain-thread route. Mail remains the owner of valid folder selection.

| Requirement | Implementation | Evidence | Remaining gap |
| --- | --- | --- | --- |
| Preserve folder and advice identity | Folder suffix in Run Monitor route and Back control | Domain coverage of all five folders; phone/desktop Inbox, Starred, Archive reload journeys | Search and scroll restoration |
| Retain incident and original view | Existing incident/view routing remains | Real Open Run, reload, Back to advice and list clicks | Persist later in-app tab changes |
| Preserve history and domain ownership | No new mutation or save schema | Navigation snapshot equality, original retry/replay/stale-link tests | Historical incident viewer |
| Compatible saved links | Optional folder suffix; safe decoding; clear unrelated context | Legacy and encoded route tests | Physical iPhone/PWA validation |

The existing NPC Mail domain test verifies routing, the real return handler, old links, encoded thread IDs, malformed routes and unchanged canonical state. The native browser journey retains NPC delivery, retry, debug bundle and stale-link coverage, adds destination reloads and folder/list checks, and compares cash, run/workstation, NPCs, incident records, gates, portfolio and Mail snapshots. Failures retain a screenshot, trace, state and event evidence. The existing landscape/tablet/wide layout checks remain.

The first browser attempt exposed career workload calculation feeding its derived value back into the canonical workload on every initialization. Repeated reloads erased consultation cost. `updateWorkloadState` now updates only the derived workload display; advice updates that projection immediately after its explicit workload mutation. A regression fails before the fix and verifies stable repeated initialization plus one consultation charge and exact retry. The browser fixture initializes model metadata before capturing a saved-state baseline and uses a real cash value. Its full state comparison is retained.

Local domain tests, full static checks and production build pass. GitHub Actions owns pinned Playwright 1.54.2 browser validation and full release/sign-off evidence; consult the PR for completed results and exact SHA. Preserve policy v19, all 45 blockers, 15 apps, 190 routes and 255 screenshot comparisons. Cache v58 refreshes existing runtime assets. No merge or deployment is automatic; physical iPhone/PWA verification remains manual.

Next bounded slice after merge: persist Run Monitor tab changes in its existing session detail without losing advice return context.

## Reviewed workload visual changes

The first full release run passed the functional gates and 190-route crawl; its only blockers were 65 exact screenshot mismatches caused by the workload correction. A separate diagnostic run captured the exact merged base under pinned Playwright 1.54.2 and reproduced **all 255 accepted screenshots with zero mismatches**. It did not change this PR's runtime or baselines.

All 65 before/after changed regions were reviewed across five viewports. Career workload percentages now show their actual derived values (11–26% in the fixture) instead of 1%. Operations coverage/outage-risk values and dependent reliability coverage reflect those workloads; company summaries and overlays display the same corrected metrics. The only dimension change is phone Career height 3040 → 3055px from wrapping the wider values. No CSS, inventory, threshold or capture rule changed.

Only those 65 reviewed expectations were promoted. The other 190 capture entries and the separate finite raster variants remain unchanged. `visual-qa/workload-baseline-review.json` records every before/after hash, dimensions, comparison bands and evidence URLs. This accepts a verified behavioral correction; it does not mask arbitrary visual failures. See the PR for the final current-head release result.
