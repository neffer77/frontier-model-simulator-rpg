name: FrontierOS Data Explorer evidence

on:
  pull_request:
    paths:
      - 'data-explorer-frontieros.js'
      - 'data-explorer-frontieros.css'
      - 'data-explorer-catalog.js'
      - 'data-explorer-command.js'
      - 'data-evals.js'
      - 'frontier-app-registry.js'
      - 'index.html'
      - 'sw.js'
      - 'tests/data-explorer-frontieros.mjs'
      - 'tests/data-explorer-frontieros-static.mjs'
      - '.github/workflows/data-explorer-frontieros.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  evidence:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install --no-audit --no-fund
      - name: Resolve Playwright version
        id: playwright-version
        run: |
          VERSION=$(node -p "require('./node_modules/playwright/package.json').version")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
      - name: Restore Playwright browser cache
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ steps.playwright-version.outputs.version }}
      - name: Install Chromium system dependencies
        timeout-minutes: 2
        continue-on-error: true
        run: npx playwright install-deps chromium
      - name: Download Chromium on cache miss
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install chromium
      - name: Report Playwright cache
        run: |
          echo "Playwright Chromium cache hit: ${{ steps.playwright-cache.outputs.cache-hit }}" >> "$GITHUB_STEP_SUMMARY"
      - name: Static Data Explorer contract
        run: node tests/data-explorer-frontieros-static.mjs
      - name: Build production site
        run: npm run build:site
      - name: Start production server
        run: python3 -m http.server 4173 --directory _site > /tmp/data-explorer-server.log 2>&1 &
      - name: Run native Data Explorer evidence
        run: node tests/data-explorer-frontieros.mjs
        env:
          TEST_URL: http://127.0.0.1:4173/
      - name: Publish summary
        if: always()
        run: |
          if [ -f artifacts/data-explorer-frontieros/REPORT.md ]; then cat artifacts/data-explorer-frontieros/REPORT.md >> "$GITHUB_STEP_SUMMARY"; fi
      - name: Upload P5.2.5 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: data-explorer-frontieros-${{ github.event.pull_request.number || github.run_number }}
          path: artifacts/data-explorer-frontieros
          if-no-files-found: warn
          retention-days: 30
