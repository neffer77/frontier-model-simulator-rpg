import {chromium} from 'playwright';
import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
const base=process.env.TEST_URL||'http://127.0.0.1:4173/';const out=path.resolve('artifacts/mobile-frontieros');fs.rmSync(out,{recursive:true,force:true});fs.mkdirSync(out,{recursive:true});const browser=await chromium.launch({headless:true});const context=await browser.newContext({viewport:{width:390,height:844},isMobile:true,hasTouch:true});await context.tracing.start({screenshots:true,snapshots:true,sources:true});const page=await context.newPage(),errors=[];page.on('pageerror',e=>errors.push(String(e?.stack||e)));await page.goto(`${base}${base.includes('?')?'&':'?'}frontieros=1`,{waitUntil:'networkidle'});await page.evaluate(()=>localStorage.clear());await page.reload({waitUntil:'networkidle'});await page.locator('.frontieros-phone-shell').waitFor({state:'visible'});
const initial=await page.evaluate(()=>frontierMobileShellSnapshot());assert.equal(initial.active,true);assert.equal(initial.view,'home');assert.equal(initial.apps.length,15,'launcher must render canonical 15-app registry');assert.equal(await page.locator('.frontieros-app-icon').count(),15,'launcher tile count drifted');assert.equal(await page.locator('.frontieros-mobile-appbar').isVisible(),false);const layout=await page.locator('.frontieros-phone-shell').evaluate(el=>({width:el.getBoundingClientRect().width,viewport:innerWidth,scrollWidth:document.documentElement.scrollWidth}));assert(layout.width<=layout.viewport+1);assert(layout.scrollWidth<=layout.viewport+1);await page.screenshot({path:path.join(out,'home-portrait.png'),fullPage:true});
await page.getByRole('button',{name:/Run Monitor/i}).click();await page.locator('.frontieros-mobile-appbar').waitFor({state:'visible'});let snap=await page.evaluate(()=>frontierMobileShellSnapshot());assert.equal(snap.view,'app');assert.equal(snap.currentApp,'training');assert(await page.locator('#app').isVisible());await page.screenshot({path:path.join(out,'run-monitor-app.png'),fullPage:true});await page.getByRole('button',{name:/Home/}).first().click();await page.locator('.frontieros-phone-shell').waitFor({state:'visible'});snap=await page.evaluate(()=>frontierMobileShellSnapshot());assert.equal(snap.view,'home');assert.equal(snap.currentApp,null);
await page.getByRole('button',{name:/Projects/i}).click();assert.equal((await page.evaluate(()=>frontierMobileShellSnapshot())).view,'home');const toast=page.locator('.frontieros-phone-toast');await toast.waitFor({state:'visible'});assert.match(await toast.textContent(),/locked|start-company/i);await page.setViewportSize({width:844,height:390});await page.evaluate(()=>dispatchEvent(new Event('resize')));await page.waitForTimeout(100);const landscape=await page.locator('.frontieros-phone-shell').evaluate(el=>({right:el.getBoundingClientRect().right,viewport:innerWidth,scrollWidth:document.documentElement.scrollWidth}));assert(landscape.right<=landscape.viewport+1);assert(landscape.scrollWidth<=landscape.viewport+1);await page.screenshot({path:path.join(out,'home-landscape.png'),fullPage:true});
const events=await page.evaluate(()=>frontierEventJournal({type:'os.mobile.*',limit:100}));assert(events.some(e=>e.type==='os.mobile.shell.ready'));assert(events.some(e=>e.type==='os.mobile.app.opened'&&e.data.appId==='training'));assert(events.some(e=>e.type==='os.mobile.app.blocked'&&e.data.appId==='projects'));assert.equal(errors.length,0,`runtime page errors: ${errors.join(' | ')}`);await context.tracing.stop({path:path.join(out,'trace.zip')});await context.close();await browser.close();const report={version:1,item:'P5.1.2',status:'pass',generatedAt:new Date().toISOString(),apps:initial.apps.length,portrait:'390x844',landscape:'844x390',events:events.length,pageErrors:errors.length,evidence:['home-portrait.png','run-monitor-app.png','home-landscape.png','trace.zip']};fs.writeFileSync(path.join(out,'report.json'),JSON.stringify(report,null,2)+'\n');fs.writeFileSync(path.join(out,'REPORT.md'),`# P5.1.2 Mobile FrontierOS Home\n\n- Status: **PASS**\n- Canonical app tiles: **${report.apps}**\n- Phone portrait + landscape: **PASS**\n- Run Monitor launch → Home return: **PASS**\n- Locked-app explanation: **PASS**\n- Runtime page errors: **0**\n`);console.log('P5.1.2 mobile FrontierOS Home regression passed');
